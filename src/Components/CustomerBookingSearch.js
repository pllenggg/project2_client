import React, { Component } from 'react';

class CustomerBookingSearch extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
        <h1>This customer booking search form, if you don't like the name. No worry just change it.</h1>
    );
  }
}

export default CustomerBookingSearch;
